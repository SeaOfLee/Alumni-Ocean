<%= form_for ([@post, @post.comments.build]) do |f| %>
  <%= f.text_area :content, :placeholder => "Enter your comment here..." %><br>
  <%= f.submit 'Add your comment', class: 'button_new' %>
<% end %>
